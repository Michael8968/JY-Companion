# Locust load test configuration for JY-Companion
# Usage: locust -f scripts/load_test.py --config scripts/locustfile.conf

[runtime settings]
host = http://localhost:8000
users = 500
spawn-rate = 50
run-time = 5m
headless = true
csv = reports/load_test
html = reports/load_test_report.html
